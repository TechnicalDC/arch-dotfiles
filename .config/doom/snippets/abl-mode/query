# -*- mode: snippet -*-
# name: create query with loop
# key: query
# --
create query ${1:queryHandle}.
${1}:set-buffers(${2}).
${1}:query-prepare(
   "${3}"
).
${1}:query-open().
${1}:get-first().

do while not ${1}:query-off-end:

   $0

   ${1}:get-next().
end.

${1}:query-close().